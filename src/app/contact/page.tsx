import ContactPage from './ContactPage';

export const metadata = {
  title: 'contact — hiral panchal',
  description: 'get in touch with hiral panchal — email, phone, linkedin, github, instagram. immediately available for full-time roles.',
};

export default function Contact() {
  return <ContactPage />;
}
