import WorkPage from './WorkPage';

export const metadata = {
  title: 'selected work — hiral panchal',
  description: 'outcome-led case studies of hiral panchal\'s best work — protein bind, ci/cd automation, crm system, and cybershield.',
};

export default function Work() {
  return <WorkPage />;
}
