import ProjectsPage from './ProjectsPage';

export const metadata = {
  title: 'projects — hiral panchal',
  description: 'all 15 projects by hiral panchal — full-stack, frontend, data engineering, and devops projects deployed across vercel, netlify, and github pages.',
};

export default function Projects() {
  return <ProjectsPage />;
}
